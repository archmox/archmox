name: Fleet Health Check

on:
  schedule:
    # Run every 30 minutes (matching the worker's cron trigger)
    - cron: '*/30 * * * *'
  workflow_dispatch:
    inputs:
      notify:
        description: 'Send notifications even if all healthy'
        required: false
        type: boolean
        default: false

concurrency:
  group: health-check
  cancel-in-progress: false

jobs:
  check-fleet:
    name: Check All Fleet Services
    runs-on: ubuntu-latest
    outputs:
      all_healthy: ${{ steps.assess.outputs.all_healthy }}
      summary: ${{ steps.assess.outputs.summary }}
      main_status: ${{ steps.main.outputs.status }}
      main_ms: ${{ steps.main.outputs.ms }}
      pve_status: ${{ steps.pve.outputs.status }}
      pve_ms: ${{ steps.pve.outputs.ms }}
      pbs_status: ${{ steps.pbs.outputs.status }}
      pbs_ms: ${{ steps.pbs.outputs.ms }}
      pmg_status: ${{ steps.pmg.outputs.status }}
      pmg_ms: ${{ steps.pmg.outputs.ms }}
      docs_status: ${{ steps.docs.outputs.status }}
      docs_ms: ${{ steps.docs.outputs.ms }}
      cdn_status: ${{ steps.cdn.outputs.status }}
      cdn_ms: ${{ steps.cdn.outputs.ms }}
    steps:
      - name: Check main site
        id: main
        run: |
          START=$(date +%s%N)
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            --connect-timeout 10 \
            https://archmox.acreetionos.org/ 2>&1)
          END=$(date +%s%N)
          MS=$(( (END - START) / 1000000 ))
          echo "status=$([ "$HTTP_CODE" = "200" ] && echo "up" || echo "down")" >> $GITHUB_OUTPUT
          echo "ms=$MS" >> $GITHUB_OUTPUT
          echo "code=$HTTP_CODE" >> $GITHUB_OUTPUT

      - name: Check PVE
        id: pve
        run: |
          START=$(date +%s%N)
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            --connect-timeout 10 \
            https://pve.archmox.acreetionos.org/ 2>&1)
          END=$(date +%s%N)
          MS=$(( (END - START) / 1000000 ))
          echo "status=$([ "$HTTP_CODE" = "200" ] && echo "up" || echo "down")" >> $GITHUB_OUTPUT
          echo "ms=$MS" >> $GITHUB_OUTPUT

      - name: Check PBS
        id: pbs
        run: |
          START=$(date +%s%N)
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            --connect-timeout 10 \
            https://pbs.archmox.acreetionos.org/ 2>&1)
          END=$(date +%s%N)
          MS=$(( (END - START) / 1000000 ))
          echo "status=$([ "$HTTP_CODE" = "200" ] && echo "up" || echo "down")" >> $GITHUB_OUTPUT
          echo "ms=$MS" >> $GITHUB_OUTPUT

      - name: Check PMG
        id: pmg
        run: |
          START=$(date +%s%N)
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            --connect-timeout 10 \
            https://pmg.archmox.acreetionos.org/ 2>&1)
          END=$(date +%s%N)
          MS=$(( (END - START) / 1000000 ))
          echo "status=$([ "$HTTP_CODE" = "200" ] && echo "up" || echo "down")" >> $GITHUB_OUTPUT
          echo "ms=$MS" >> $GITHUB_OUTPUT

      - name: Check Docs
        id: docs
        run: |
          START=$(date +%s%N)
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            --connect-timeout 10 \
            https://docs.archmox.acreetionos.org/ 2>&1)
          END=$(date +%s%N)
          MS=$(( (END - START) / 1000000 ))
          echo "status=$([ "$HTTP_CODE" = "200" ] && echo "up" || echo "down")" >> $GITHUB_OUTPUT
          echo "ms=$MS" >> $GITHUB_OUTPUT

      - name: Check CDN
        id: cdn
        run: |
          START=$(date +%s%N)
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            --connect-timeout 10 \
            https://cdn.archmox.acreetionos.org/ 2>&1)
          END=$(date +%s%N)
          MS=$(( (END - START) / 1000000 ))
          echo "status=$([ "$HTTP_CODE" = "200" ] && echo "up" || echo "down")" >> $GITHUB_OUTPUT
          echo "ms=$MS" >> $GITHUB_OUTPUT

      - name: Check API Health
        id: api
        run: |
          RESP=$(curl -s --connect-timeout 10 \
            https://archmox.acreetionos.org/api/health 2>&1)
          echo "response<<EOF" >> $GITHUB_OUTPUT
          echo "$RESP" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Assess overall health
        id: assess
        run: |
          ALL_UP=true
          SUMMARY=""

          for service in main pve pbs pmg docs cdn; do
            STATUS="${{ steps[service].outputs.status }}"
            MS="${{ steps[service].outputs.ms }}"
            if [ "$STATUS" != "up" ]; then
              ALL_UP=false
              SUMMARY="$SUMMARY\n  ❌ $service: $STATUS (${MS}ms)"
            else
              SUMMARY="$SUMMARY\n  ✅ $service: $STATUS (${MS}ms)"
            fi
          done

          echo "all_healthy=$ALL_UP" >> $GITHUB_OUTPUT
          echo "summary=$SUMMARY" >> $GITHUB_OUTPUT

          echo "::notice::Fleet Health Summary:$SUMMARY"

  notify-health:
    name: Notify on Health Issues
    needs: check-fleet
    if: |
      always() &&
      (needs.check-fleet.outputs.all_healthy == 'false' ||
       github.event.inputs.notify == 'true')
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord notification
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        if: env.DISCORD_WEBHOOK != ''
        run: |
          HEALTHY="${{ needs.check-fleet.outputs.all_healthy }}"
          SUMMARY="${{ needs.check-fleet.outputs.summary }}"
          STATUS_ICON="$([ "$HEALTHY" = "true" ] && echo "✅" || echo "❌")"
          curl -X POST -H "Content-Type: application/json" \
            -d "{
              \"content\": \"${STATUS_ICON} **Archmox Fleet Health Check**\n\`\`\`\n${SUMMARY}\n\`\`\`\"
            }" \
            "$DISCORD_WEBHOOK"

  record-metrics:
    name: Record Health Metrics
    needs: check-fleet
    runs-on: ubuntu-latest
    steps:
      - name: Store in KV (via API)
        run: |
          curl -s -X POST \
            "https://archmox.acreetionos.org/api/health/record" \
            -H "Content-Type: application/json" \
            -d '{
              "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
              "source": "github-actions",
              "results": {
                "main": "${{ needs.check-fleet.outputs.main_status }}-${{ needs.check-fleet.outputs.main_ms }}ms",
                "pve": "${{ needs.check-fleet.outputs.pve_status }}-${{ needs.check-fleet.outputs.pve_ms }}ms",
                "pbs": "${{ needs.check-fleet.outputs.pbs_status }}-${{ needs.check-fleet.outputs.pbs_ms }}ms",
                "pmg": "${{ needs.check-fleet.outputs.pmg_status }}-${{ needs.check-fleet.outputs.pmg_ms }}ms",
                "docs": "${{ needs.check-fleet.outputs.docs_status }}-${{ needs.check-fleet.outputs.docs_ms }}ms",
                "cdn": "${{ needs.check-fleet.outputs.cdn_status }}-${{ needs.check-fleet.outputs.cdn_ms }}ms"
              }
            }' || true
